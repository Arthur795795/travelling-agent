# 旅行 Agent 开发 Spec

状态：**已确认**  
决策范围：D1–D160  
约束：本文件是实现基线；任何范围变更必须单独提出，不得在实现中静默修改。

## 1. 题目目标

实现一个面向中国境内城市自由行的旅行规划 Agent。

用户输入出发地、目的地、日期、人数、预算、偏好和已预订项目后，系统通过 DeepSeek 调用地图、搜索和官方信息源，生成一份：

- 结构化、可编辑的 3–5 日行程；
- 时间和空间上可执行；
- 包含交通、住宿、景点、餐饮、预算和备选方案；
- 关键事实附来源、查询时间和核验状态；
- 支持锁定项目、局部重规划、分享、PDF 及日历导出。

产品重点不是生成攻略文案，而是证明“澄清需求—调用工具—核验事实—规划—校验—修复—解释”的完整 Agent 闭环。

## 2. 已知信息

### 2.1 当前工作区

在确认需求后、建立本文档前，工作区状态为：

- 没有 README；
- 没有代码；
- 没有依赖配置；
- 没有测试；
- 不是 Git 仓库；
- 不存在需要兼容的已有模块或运行命令。

因此，本文件中的目录、接口和命令是拟建立的工程约定。

### 2.2 输入

完整规划前至少需要：

- 出发地；
- 目的地；
- 日期或 3–5 天行程长度；
- 同行人数及儿童、老人等结构；
- 预算水平或上限；
- 已预订酒店、车次、航班；
- 不可违反的硬约束；
- 旅行偏好。

目的地未确定时，先生成 2–3 个候选目的地，不直接生成完整行程。

禁止要求或保存姓名、手机号、身份证、护照、支付信息、完整订单号和精确家庭住址。

### 2.3 输出

核心输出是结构化行程对象，并渲染为：

- 每日时间线；
- 交通段与缓冲时间；
- 地点及空间关系；
- 分类预算；
- 已预订和锁定项目；
- 预约、开放时间等证据；
- 冲突及风险提醒；
- 主方案和关键备选；
- 分享页；
- PDF；
- `.ics` 日历。

### 2.4 产品范围

- 完整支持北京、上海、重庆、西安、杭州；
- 其他国内城市只能标记为实验性草案；
- 完整模式支持 3–5 天、1–6 人、单一住宿城市；
- 最多包含一个单程约 2 小时以内的周边一日游；
- 日期可在未来 12 个月内；
- 城际交通重点支持国内火车和航班；
- 行程边界从离开出发地到返回出发地。

### 2.5 业务规则

- 不绑定携程账户，不读取订单；
- 不预订、不支付、不声称读取实时库存；
- 平台链接标注查询时间和搜索条件；
- 用户录入的预订默认锁定；
- Agent 不得未经确认修改硬约束和锁定项目；
- 同一关键冲突最多自动修复两轮；
- 关键事实无法核验时显示“需复核”或“查询失败”；
- 存在未解决关键问题时，行程不能进入“可执行”状态。

### 2.6 运行方式

- Next.js + TypeScript；
- 单仓库、单 Docker 容器；
- Web/API 与任务执行器同容器、模块隔离；
- SQLite 持久化任务和分享数据；
- 浏览器保存普通匿名行程；
- 当前受限演示部署在香港节点；
- 完成相关手续后再迁移中国大陆节点。

模型和密钥：

- 使用 DeepSeek 官方 Responses API；
- 核心模型为 `deepseek-v4-pro`；
- 用户自带 DeepSeek Key；
- 产品方提供自己的高德 Web 服务 Key；
- DeepSeek Key 只在页面内存和单次后端请求内存在；
- 无 Key 可以体验固定案例和预设重规划。

### 2.7 测试方式

项目需要新建：

- 单元测试；
- 服务集成测试；
- 浏览器端到端测试；
- 固定数据 Agent 评测；
- 少量真实联网评测；
- 故障注入测试；
- 安全与隐私检查；
- 5 人真实用户测试。

## 3. 功能需求

### F1. 首页与入口

提供“体验北京四日案例”和“规划我的旅行”两个入口。案例无需 DeepSeek Key；自定义规划需要用户自己的 Key。

完成标准：无 Key 可完整浏览案例、创建个人副本并恢复基线；界面明确区分固定案例和真实生成。

### F2. 需求收集与旅行简报

采用自然语言输入、快捷字段和持续可见的旅行简报。Agent 每轮集中提出 3–5 个当前可回答的高影响问题；次要信息允许跳过并使用明确披露的默认值。

开始完整规划前必须明确出发地、目的地、日期/天数、同行结构、预算、已预订项目和硬约束。用户可直接修改旅行简报。

### F3. BYOK 密钥流程

提供专用 DeepSeek Key 输入框，进行最小验证调用，显示连接状态和清除操作。

Key 不得进入 URL、LocalStorage、SQLite、日志、分析事件或错误报告。刷新或关闭页面后 Key 消失。后端仅在处理请求期间持有 Key。

### F4. Agent 状态机

固定阶段：

1. `requirements_check`
2. `skeleton_planning`
3. `evidence_collection`
4. `route_and_budget`
5. `hard_validation`
6. `repair`
7. `finalization`

每阶段保存状态、输入摘要、输出摘要、耗时和费用统计。服务重启后可以读取已完成步骤。因 BYOK Key 不持久化，需要继续模型调用的任务进入 `waiting_for_credentials`。

### F5. DeepSeek 适配器

封装 Responses API 的流式响应、Function Calling、Token/费用统计、超时、限流、错误归类和模型版本记录。

模型通过受约束的最终提交工具返回结构化行程；所有工具参数执行前经过运行时 Schema 校验。模型失败不得生成伪造的完整行程。

### F6. 工具系统

具名工具至少包括：

- 地点搜索与详情；
- 路线计算；
- 天气；
- 联网搜索；
- 官方页面读取；
- 平台搜索/预订跳转生成。

工具按 Agent 阶段开放，不提供通用服务器命令或无限制 URL 请求。每次结果包含来源、查询时间和状态。

### F7. 事实证据系统

关键事实拆成 Claim，并关联 Evidence，包括开放时间、闭馆日、预约、地址、交通时间、天气、费用范围和平台查询条件。

来源优先级：

1. 官方场馆或承运方；
2. 地图、交通等专用 API；
3. 正规平台；
4. 普通网页或搜索摘要。

每个关键 Claim 包含状态和查询时间；来源冲突必须展示；关键 Claim 缺少可靠证据时不得标记完全可执行。

### F8. 结构化行程

结构化行程是唯一权威对象，至少包括：

```text
Trip
├── brief
├── assumptions
├── lockedBookings
├── days[]
│   ├── activities[]
│   └── legs[]
├── budget
├── evidence[]
├── alerts[]
├── alternatives[]
├── lifecycleStatus
└── version
```

所有实体拥有稳定 ID。页面、分享、PDF 和日历必须使用同一结构化对象，不维护独立漂移的文本版本。

### F9. 行程内容标准

- 默认适中密度：上午、下午各一个核心活动，附近项目作为可选；
- 允许为了休息、排队、购物或意外主动留白；
- 每段活动包含时间窗、地点、持续时间、交通、费用、预约、理由和备选；
- 固定预订使用精确时间，普通活动使用弹性时间窗；
- 普通市内移动增加约 15–20 分钟缓冲；
- 预约景点提前约 20–30 分钟；
- 火车默认提前 45–60 分钟到站；
- 国内航班默认提前约 120 分钟到机场；
- 老人、儿童、行李和大型枢纽增加余量。

### F10. 交通、住宿、餐饮与预算

未预订交通：能核实时给一个推荐班次和备用候选；不能核实时只给建议时段、筛选条件和平台跳转，不声称实时余票或价格。

住宿：先推荐区域，再给最多三个候选，展示位置、预算档、交通便利性和理由，不承诺房态。

餐饮：使用时间窗，重要餐次给一个主选和一个附近备选；过敏和忌口是硬约束。

预算：区分预算上限、估算范围、已付款实际费用和未知费用；按城际交通、住宿、门票、餐饮和市内交通分类，默认保留约 10% 机动金。

### F11. 硬门槛校验

确定性校验至少覆盖：

- 必填字段；
- 时间重叠；
- 营业与预约时间；
- 交通时间和缓冲；
- 固定预订冲突；
- 行程密度；
- 预算上限；
- 证据完整性；
- 产品支持范围；
- 禁止的实时库存措辞。

硬错误与软建议分离。未解决硬错误阻止 `executable`。锁定项目不得被自动修复修改。

### F12. 行程呈现

规划工作区包括对话入口、可编辑旅行简报、时间线、简化路线总览、预算、风险和证据。

时间线是主界面。获得适当许可前不嵌入完整高德地图瓦片，仅展示简化空间关系及逐段“在高德打开”。桌面端优先，移动端支持查看和基础编辑。

### F13. 编辑、锁定与局部重规划

支持修改、拖动、锁定/解锁和用户主动触发的途中重规划。

无连锁影响的小修改可以直接生效；跨时段、预算、锁定项目或多日的变化先展示影响。修复保留不受影响的日期和项目，同一冲突最多修复两轮，失败后提供 1–2 个用户选择。

### F14. 预设案例

提供经过人工核验的北京四日案例，以及 2–3 个预先验证的重规划情境，例如下雨、未预约成功、临时疲劳或酒店变化。

无 Key 可查看、编辑结构化字段和执行预设情境；自由文本重规划需要用户 Key。用户修改个人副本，不影响公共基线。

### F15. 分享与导出

- 不可猜测的只读分享令牌；
- 独立删除令牌；
- 默认 30 天过期；
- `noindex`；
- 创建前选择分享字段；
- 默认隐藏聊天、私人备注和预算；
- 支持 PDF、`.ics`、外部地图及平台链接。

所有导出从结构化行程确定性生成，并包含 AI 生成标识。

### F16. 匿名保存与离线

普通行程默认保存在当前浏览器约 30 天，提供清除操作和共享设备提示。已生成行程可以只读查看；完整 PWA 离线编辑和多端同步不在 MVP 范围。离线只读可按 D159 延后。

### F17. 安全、限流与运行控制

- 匿名会话与每日轮换 IP 哈希限流；
- 密钥、证件和订单字段脱敏；
- 安全 URL 获取；
- 工具调用和费用预算；
- 自定义生成、联网搜索、高德、分享和导出独立开关；
- 结构化、无正文的运行指标；
- SQLite 每日加密备份，保留 7 天。

临时任务 24 小时清理，分享 30 天，脱敏运行指标 30 天。

### F18. 案例研究与评测页

公开展示问题定义、决策过程、Agent 工作流、数据来源、评测方法、真实指标、失败案例和已知限制。必须标明模型、日期、样本数和通过率，不只展示最佳样本。

## 4. 核心数据结构

至少定义：

- `TripBrief`
- `Party`
- `BudgetConstraint`
- `BookedItem`
- `Trip`
- `TripDay`
- `Activity`
- `TransportLeg`
- `Place`
- `CostItem`
- `Evidence`
- `Claim`
- `Alert`
- `Alternative`
- `ValidationIssue`
- `TripRevision`
- `PlanningJob`
- `ShareRecord`

关键状态：

```text
EvidenceStatus:
verified | recheck_required | failed

TripLifecycle:
draft | checked | executable | blocked

JobStatus:
queued | running | waiting_for_credentials |
completed | failed | cancelled

Severity:
info | warning | blocking
```

金额统一记录币种、精确值或范围、来源和可信度。时间使用 ISO 时间并明确 `Asia/Shanghai`。

## 5. 核心流程

1. 前端提交结构化 `TripBrief`；
2. 后端检查支持范围、字段和频率；
3. 验证 DeepSeek Key；
4. 创建 `PlanningJob`；
5. Agent 生成不含未经核验细节的骨架；
6. 按阶段开放并调用具名工具；
7. 工具结果转换为标准化 Evidence 和 Claim；
8. Agent 填充活动、交通和预算；
9. 硬校验器检查；
10. 将结构化问题返回模型，最多修复两轮；
11. 再次确定性校验；
12. 生成 `draft`、`checked`、`executable` 或 `blocked`；
13. 把最终对象发送给浏览器保存；
14. 服务端临时数据按期限清理。

## 6. 建议接口

```text
POST   /api/keys/deepseek/validate
POST   /api/planning-jobs
GET    /api/planning-jobs/:id
GET    /api/planning-jobs/:id/events
POST   /api/planning-jobs/:id/resume
POST   /api/planning-jobs/:id/cancel

POST   /api/trips/:id/change-preview
POST   /api/trips/:id/changes
POST   /api/trips/:id/replan

POST   /api/shares
DELETE /api/shares/:deleteToken
GET    /share/:readToken

GET    /api/trips/:id/export.pdf
GET    /api/trips/:id/export.ics
```

进度优先使用 SSE，连接失败时退回轮询。用户 Key 不写入 `PlanningJob`。

## 7. 数据源边界

- DeepSeek 搜索用于发现，不直接等于事实；
- 高德用于地址、POI、路线和适用范围内天气；
- 官方页面核对开放、预约和特殊规则；
- OTA 链接只表达搜索条件，不表达库存；
- 高德原始结果不得在许可未确认时建立跨用户缓存；
- 官方网页读取不绕过登录、验证码或反爬；
- 无法可靠读取时标记失败。

## 8. 数据、隐私与发布

服务端保存：

- 已完成任务临时内容：24 小时内清理；
- 分享页：30 天；
- 脱敏运行指标：30 天；
- 加密备份：7 天轮换。

生成界面显示 DeepSeek 模型名称和“AI 辅助生成”。关于/产品详情页显示模型名称、服务提供方以及经核实的备案号或上线编号。分享页和导出文件也包含生成标识。

“先公开”的边界：案例研究和固定演示可公开，自定义生成只向定向测试者开放；当前部署香港节点，同时推进 ICP、生成式 AI 应用登记核对和高德许可，完成后再扩大开放。

## 9. 性能与成本

- 2 秒内显示任务进度；
- 完整核验计划中位数不超过 75 秒；
- 95 分位不超过 150 秒；
- 180 秒后结束或提供可恢复结果；
- 单次模型估算成本平均不超过 2 元；
- 95 分位不超过 5 元；
- 产品方月度 300 元预警，500 元硬停止新生成任务。

访客 DeepSeek 费用由访客自己的 Key 承担。产品预算覆盖托管、高德调用和内部评测。

## 10. 测试与验收

需要建立以下脚本：

```bash
npm run lint
npm run typecheck
npm run test
npm run test:integration
npm run test:e2e
npm run test:eval:fixtures
npm run test:eval:live
npm run test:security
npm run build
```

### 10.1 标准评测

12 个案例各运行 3 次，共 36 次。约 8 个北京变体和 4 个其他城市案例，覆盖儿童、老人、预算、下雨、闭馆、晚抵达、锁定预订和来源故障。

关键失败必须为 0：

- 修改锁定预订；
- 明显时间或空间不可达；
- 违反硬约束；
- 伪造来源或核验结果；
- 声称读取实时库存；
- 隐藏关键冲突。

完整任务成功率至少 90%，软评分平均至少 80 分。软评分权重：个性化 25%、路线效率 25%、完整可执行性 20%、预算质量 15%、解释与证据 15%。

### 10.2 故障注入

模拟 DeepSeek 429/超时/无效 JSON、高德超时或额度耗尽、官网 404/超时/结构变化、来源冲突、PDF 失败、SQLite 写入失败、SSE 断线、容器重启和 BYOK Key 丢失。

正确行为是可见降级、保留状态或明确阻塞，不得编造答案。

### 10.3 真实用户

5 名目标用户分别修改北京案例并规划自己的旅行。至少 4 人无需指导完成；获得可接受方案的中位时间不超过 8 分钟；关键修改不超过 2 分钟。

### 10.4 模型更新

DeepSeek 滚动模型变化后先跑 5 个核心冒烟案例；出现关键失败则暂停自定义生成，完整 36 次回归通过后恢复或扩大开放。

## 11. 不做什么

MVP 不实现：

- 用户注册、登录和账户同步；
- 携程/飞猪账户或订单绑定；
- 代订、支付、退改；
- 实时余票、房态或价格保证；
- 主动后台监控和通知；
- 国际旅行、签证和多城连续旅行；
- 7 人以上团队方案；
- 完整无障碍承诺；
- 社区、评论区和公开行程广场；
- 运营管理后台；
- 返佣和商业排序；
- 多模型动态切换或静默降级；
- 完整离线编辑和同步；
- 未获许可的地图嵌入或数据缓存；
- 任意网页抓取、验证码绕过和登录态自动化；
- 原始模型思维链展示；
- 为通过校验而自动改变硬约束。

## 12. 开发顺序与裁剪

1. 北京案例的端到端纵向闭环；
2. 其余四个完整支持城市；
3. 评测、案例研究和公开展示。

时间不足时优先延后视觉润色、额外城市完整支持、复杂拖拽和离线只读；不得裁掉事实核验、硬校验、来源状态、锁定保护和密钥安全。延后的城市必须如实标注，不能提前宣称完整支持。

## 13. MVP 完成定义

- 北京闭环完整；
- 五城支持范围如实标注；
- 关键流程可用；
- 36 次评测达到门槛；
- 真实用户测试完成；
- 无已知关键失败；
- 成本、隐私和密钥护栏生效；
- README、架构说明、评测结果和案例页齐全；
- 生产构建成功。

## 14. 需在实施阶段核实的外部事实

- DeepSeek 当前适用的准确备案号或上线编号；
- 本应用是否以及如何办理地方登记；
- 大陆域名和 ICP 流程；
- 高德公开展示、缓存和商业使用许可；
- 各平台跳转参数及最新条款；
- 五个支持城市官方来源的可访问性。
