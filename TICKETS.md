# 旅行 Agent 开发 Tickets

状态：基于已确认的 [SPEC.md](./SPEC.md) 拆分，尚未开始实现。

## 拆分思路

Tickets 采用“领域基础 → 外部能力 → Agent 核心 → 用户闭环 → 安全边界 → 评测发布”的顺序。先建立可独立测试的数据契约和确定性规则，再接入 DeepSeek、高德和网页来源；之后完成北京纵向闭环，最后增加分享、导出、五城评测、部署与发布材料。

每个 ticket 只覆盖一个主要行为边界，测试与实现放在同一 ticket。测试从公开函数、HTTP 接口或页面行为观察结果，不锁定私有方法或内部组件结构。

## 执行顺序

1. **基础（001–009）**：工程、领域契约、持久化、安全原语和证据模型。
2. **核心能力（010–026）**：BYOK、模型与工具适配、规划状态机、校验、修复和任务执行。
3. **产品闭环（027–039）**：入口、规划工作区、编辑、北京案例、分享、导出和 AI 标识。
4. **边界与保障（040–046）**：清理、限流、分析、无障碍、离线、故障与安全回归。
5. **评测与收尾（047–056）**：标准案例、联网评测、性能、模型回归、案例页、部署和发布门槛。

## 最大风险点

- **BYOK 与恢复冲突**：Key 不持久化，重启后只能恢复到 `waiting_for_credentials`。
- **事实可信度**：搜索结果、官网、高德和平台信息会缺失或冲突，不能让模型静默补全。
- **锁定项的间接破坏**：即使预订本身没变，前序安排也可能让用户无法准时抵达。
- **高德许可**：缓存、展示和再利用必须服从实际许可，不能以技术可行为依据。
- **DeepSeek 滚动版本**：模型行为可能改变，需要自动回归闸门。
- **密钥与隐私**：Key、个人信息、原始 IP 和旅行正文不能进入长期日志。
- **评测可信度**：必须保留失败样本和固定基准，不能只挑最好结果。

---

## Ticket 001：初始化工程与质量命令

### 目标

建立可运行的 Next.js + TypeScript 工程和统一质量脚本，不实现产品功能。

### 背景

来自 Spec 2.1、2.6、10。当前目录没有代码、依赖、测试或 Git 仓库，后续 ticket 需要稳定的工程基线。

### 修改范围

项目清单、TypeScript 配置、Next.js 基础结构、测试配置、忽略文件和空白首页。

### 任务

- 初始化 Next.js + TypeScript。
- 配置 lint、类型检查、单元/集成/E2E 测试入口。
- 建立 `npm run lint`、`typecheck`、`test`、`test:integration`、`test:e2e`、`test:eval:fixtures`、`test:eval:live`、`test:security`、`build`。
- 提供不会包含真实密钥的环境变量示例。
- 为测试分层设置独立目录与默认无网络模式。

### 验收标准

- 新环境按 README 安装后能够启动空白应用。
- 所有质量命令存在并以零退出码完成。
- 默认测试和构建不要求外部密钥或网络。

### 测试要求

- 增加一个访问首页的冒烟测试。
- 增加一个浏览器冒烟测试，确认页面可加载。
- CI 行为只检查公开命令结果。

### 依赖

无。

### 不包含

不实现旅行页面、数据库、模型、地图或部署。

---

## Ticket 002：定义领域 Schema 与状态枚举

### 目标

建立 Trip、TripBrief、活动、交通、证据、预算、预订、校验问题和任务状态的运行时 Schema。

### 背景

来自 Spec F8、4。结构化行程是页面、Agent、分享和导出的唯一权威对象。

### 修改范围

`domain` 模块、Schema 版本、序列化格式和领域契约测试。

### 任务

- 定义 Spec 列出的核心实体和稳定 ID。
- 定义 EvidenceStatus、TripLifecycle、JobStatus、Severity。
- 明确 ISO 时间、`Asia/Shanghai`、金额范围、币种和未知值表示。
- 为 Schema 添加版本字段和向后兼容入口。
- 提供合法与非法对象的公开解析接口。

### 验收标准

- 合法完整行程可以解析、序列化并再次解析。
- 缺失关键字段、非法时间、非法金额和未知枚举被拒绝并返回可定位错误。
- 未知费用可以表达，而不被强制转换为零。

### 测试要求

- 从调用者角度测试合法/非法载荷及错误路径。
- 覆盖跨日时间、费用范围、锁定预订和证据引用。
- 不断言 Schema 库内部实现。

### 依赖

Ticket 001。

### 不包含

不实现数据库、Agent 或业务校验规则。

---

## Ticket 003：建立 SQLite 数据模型与仓储接口

### 目标

持久化 PlanningJob、临时 Trip、TripRevision、ShareRecord 和运行元数据。

### 背景

来自 Spec F4、F15、F17。任务需要跨请求恢复，分享页需要服务端读取，但普通匿名编辑仍以浏览器为主。

### 修改范围

SQLite 初始化、迁移、仓储接口、事务边界和数据库集成测试。

### 任务

- 建立带版本的迁移机制。
- 保存任务阶段、状态、脱敏摘要、费用与时间统计。
- 保存临时 Trip、修订和分享记录。
- 令牌只保存哈希，不保存原始查看/删除令牌。
- 明确创建、读取、更新、过期和删除行为。

### 验收标准

- 重启数据库连接后可读取任务与已完成阶段。
- 同一版本并发更新不会静默覆盖。
- 数据库中不存在 DeepSeek Key 字段。
- 过期数据可被查询为不可用。

### 测试要求

- 使用临时数据库测试公开仓储行为。
- 覆盖并发版本冲突、过期、删除和迁移。
- 不测试 SQL 字符串或私有表结构细节。

### 依赖

Ticket 002。

### 不包含

不实现清理定时任务、加密备份或 API 页面。

---

## Ticket 004：实现浏览器匿名会话与本地行程存储

### 目标

让普通匿名行程和当前会话在浏览器保存约 30 天，并支持主动清除。

### 背景

来自 Spec F16。产品没有账户系统，但用户需要关闭页面后继续查看和编辑。

### 修改范围

浏览器存储适配器、会话 ID、过期策略和公开调用接口。

### 任务

- 创建匿名会话 ID，不使用稳定设备指纹。
- 保存符合 Schema 的 Trip 和修订摘要。
- 到期自动清除，用户可立即清除。
- 对损坏或旧版本数据安全降级。
- 不保存 DeepSeek Key。

### 验收标准

- 刷新后可恢复未过期行程。
- 过期、损坏或不兼容数据不会导致页面崩溃。
- 清除操作删除所有产品本地数据。
- Key 永远不出现在持久化内容中。

### 测试要求

- 通过公开存储行为测试保存、恢复、过期、清除和损坏数据。
- 增加浏览器测试确认刷新后的可见结果。

### 依赖

Ticket 002。

### 不包含

不实现账户同步、跨设备同步或完整离线应用。

---

## Ticket 005：建立密钥与敏感信息保护原语

### 目标

提供统一的 Key/个人信息检测、脱敏和禁止持久化规则。

### 背景

来自 Spec F3、F17 和“不做什么”。BYOK 是高风险入口，保护逻辑必须先于模型接入。

### 修改范围

`security` 模块、日志序列化、敏感字段检查和错误输出。

### 任务

- 检测疑似 API Key、证件号、完整订单号和支付字段。
- 为请求头、对象、错误和日志提供递归脱敏。
- 定义专用 Secret 类型，阻止普通序列化。
- 提供聊天输入拒绝/提示结果。
- 为环境变量缺失提供不泄密错误。

### 验收标准

- 敏感测试值不会出现在日志字符串、错误响应或序列化对象中。
- 聊天输入中的疑似 Key 被拒绝并引导至专用输入框。
- 普通旅行日期、预算和车次不会被误判为证件信息。

### 测试要求

- 从日志、HTTP 错误和输入验证的外部输出验证脱敏。
- 覆盖嵌套对象、异常对象和多种常见密钥格式。

### 依赖

Ticket 001。

### 不包含

不实现 DeepSeek 调用、日志平台或法律身份验证。

---

## Ticket 006：实现安全的外部页面读取器

### 目标

建立只能读取公开 HTTP/HTTPS 页面、能够阻止 SSRF 和资源滥用的获取接口。

### 背景

来自 Spec F6、F17、7。官方页面核验需要网络读取，但不得访问本机、内网、危险协议或无限大响应。

### 修改范围

URL 校验、DNS/IP 检查、重定向、超时、内容类型和响应大小限制。

### 任务

- 仅接受 HTTP/HTTPS。
- 拦截本机、内网、链路本地和保留地址。
- 每次重定向重新校验目标。
- 限制重定向次数、响应大小、内容类型和总耗时。
- 不接受用户自定义认证头，不绕过验证码或登录。

### 验收标准

- 公共测试页面可以读取并返回受限内容。
- 直接或重定向访问内网均被拒绝。
- 超限响应、非允许类型和超时产生明确失败状态。

### 测试要求

- 使用本地可控 HTTP fixture 测试公开行为。
- 覆盖 IPv4/IPv6、本机别名、重定向链和超大响应。

### 依赖

Ticket 001、Ticket 005。

### 不包含

不实现网页内容抽取、搜索或浏览器自动化。

---

## Ticket 007：建立功能开关、配额与费用计量契约

### 目标

定义生成、搜索、高德、分享、导出开关以及单任务/月度费用护栏。

### 背景

来自 Spec F17、9。外部调用必须可独立关闭，并保护访客 Key 和产品方预算。

### 修改范围

配置模块、配额状态、费用事件和公开策略接口。

### 任务

- 定义五类独立功能开关。
- 定义步骤、搜索、修复、Token 和估算费用上限。
- 定义产品方 300 元预警、500 元硬停止状态。
- 区分访客模型费用与产品方费用。
- 返回可展示的拒绝/降级原因。

### 验收标准

- 任一功能关闭时只阻止对应能力。
- 达到费用或步骤上限后不继续产生外部调用。
- 已有行程和固定案例在新生成关闭时仍可访问。

### 测试要求

- 通过公开策略接口测试开关组合和边界值。
- 验证用户模型费用不会计入产品方月度总额。

### 依赖

Ticket 002。

### 不包含

不实现 IP 限流、中间件或真实供应商账单同步。

---

## Ticket 008：实现 PlanningJob 生命周期仓储行为

### 目标

支持任务创建、阶段推进、失败、取消、完成和等待凭据状态。

### 背景

来自 Spec F4、5。长任务必须可恢复，且 BYOK Key 丢失时不能假装继续。

### 修改范围

任务服务、状态转换、乐观并发和阶段结果摘要。

### 任务

- 建立允许的状态转换表。
- 持久化每个阶段的开始、结束、结果摘要和费用。
- 支持取消请求与终态幂等。
- 支持 `waiting_for_credentials` 及恢复条件。
- 拒绝非法倒退或重复完成。

### 验收标准

- 合法任务可以完整推进。
- 重复请求不会产生重复终态。
- 进程恢复后能确定下一可执行阶段。
- 没有凭据时需要模型的任务只能等待。

### 测试要求

- 从任务服务公开 API 测试所有合法/非法转换。
- 覆盖并发推进、重复取消和终态恢复。

### 依赖

Ticket 002、Ticket 003。

### 不包含

不运行 Agent，不提供 HTTP 或 SSE 接口。

---

## Ticket 009：定义 Claim、Evidence 与来源优先级行为

### 目标

建立可复用的证据关联、核验状态和来源冲突模型。

### 背景

来自 Spec F7。关键事实必须能追溯到具体来源，搜索摘要不能直接成为最高可信证据。

### 修改范围

证据领域服务、来源类别、Claim 关联和冲突结果。

### 任务

- 定义来源类别和固定优先级。
- 将一个 Claim 关联到一个或多个 Evidence。
- 定义 verified、recheck_required、failed 的判定输入。
- 表达来源冲突及无法裁决状态。
- 输出适合 UI 和校验器消费的摘要。

### 验收标准

- 同一事实的相同证据可以合并。
- 官方与普通网页冲突时不会默认采信普通网页。
- 无法解决的关键冲突产生明确状态和关联来源。

### 测试要求

- 使用公开领域接口测试来源组合、冲突和无证据场景。
- 不断言内部排序算法的私有步骤。

### 依赖

Ticket 002。

### 不包含

不访问网络、不渲染证据 UI。

---

## Ticket 010：实现 DeepSeek Key 临时验证接口

### 目标

验证访客提供的 DeepSeek Key，并保证 Key 不被持久化或泄漏。

### 背景

来自 Spec F3。自定义规划必须在长任务开始前区分无效 Key、余额/权限问题和网络失败。

### 修改范围

`POST /api/keys/deepseek/validate`、内存凭据传递、错误映射和审计元数据。

### 任务

- 接收专用请求头或请求体 Secret 字段。
- 执行最小、受限的官方 API 验证调用。
- 返回可用、认证失败、额度/权限、限流、网络失败等稳定错误码。
- 对所有日志与异常执行脱敏。
- 确认请求结束后不保留 Key。

### 验收标准

- 合法 Key 返回可用状态。
- 各类供应商错误映射为稳定公开结果。
- 数据库、日志和响应不含完整 Key。

### 测试要求

- 使用模拟 DeepSeek 服务测试 HTTP 外部行为。
- 增加 Key 泄漏扫描测试。
- 默认测试不调用真实 DeepSeek。

### 依赖

Ticket 005、Ticket 007。

### 不包含

不保存 Key、不创建任务、不实现规划。

---

## Ticket 011：实现 DeepSeek Responses 适配器

### 目标

封装流式响应、工具调用、模型元数据、Token、费用和供应商错误。

### 背景

来自 Spec F5。Agent 不应直接依赖供应商原始响应格式。

### 修改范围

`providers/deepseek`、供应商事件到内部事件的转换、超时和取消。

### 任务

- 调用 `deepseek-v4-pro` Responses API。
- 支持流式文本、reasoning、function_call 和 web_search 事件，但不向用户暴露原始 reasoning。
- 记录返回模型信息、Token 和估算费用。
- 支持超时、取消、429、认证和不完整响应。
- 为工具回传建立稳定接口。

### 验收标准

- 模拟流可以转换为稳定内部事件。
- 原始思维内容不进入面向用户的输出或日志。
- 取消和超时停止后续读取。
- 费用统计可由公开结果读取。

### 测试要求

- 使用录制/模拟响应覆盖文本、工具、失败、截断和取消。
- 只断言适配器公开事件和结果。

### 依赖

Ticket 005、Ticket 007、Ticket 010。

### 不包含

不定义旅行工具或规划提示。

---

## Ticket 012：实现受约束的 Agent 工具注册表

### 目标

让模型只能调用当前阶段允许的具名工具，并在执行前验证参数。

### 背景

来自 Spec F6。模型可能生成非法 JSON、额外字段或调用不该开放的工具。

### 修改范围

工具定义、运行时 Schema、阶段白名单、执行结果和错误类型。

### 任务

- 定义工具注册与执行契约。
- 为每个阶段配置 allowed tools。
- 拒绝未知工具、Schema 外字段和非法参数。
- 加入调用 ID、幂等键、耗时与费用事件。
- 统一返回成功、可重试、不可重试和阻塞结果。

### 验收标准

- 合法工具调用执行一次并返回标准结果。
- 未授权、非法和重复调用不会触发外部副作用。
- 工具结果可关联到任务阶段和 Evidence。

### 测试要求

- 从模型工具调用输入测试允许、拒绝、重复和超时行为。
- 不测试注册表内部数据结构。

### 依赖

Ticket 002、Ticket 007、Ticket 011。

### 不包含

不实现具体高德、搜索或链接工具。

---

## Ticket 013：实现高德地点搜索与详情适配器

### 目标

通过产品方高德 Web 服务 Key 获取地点候选与标准化详情。

### 背景

来自 Spec F6、7。高德是主要地理底座，但原始结果不能在许可未确认时跨用户缓存。

### 修改范围

高德地点 API 客户端、错误映射、标准 Place 输出和工具注册。

### 任务

- 支持关键词/城市地点搜索和地点详情。
- 标准化名称、地址、经纬度、类别和来源时间。
- 处理无结果、歧义、额度、认证和网络错误。
- 使用服务端环境 Key，不接受访客高德 Key。
- 不建立跨用户原始响应缓存。

### 验收标准

- 合法查询返回标准 Place 候选。
- 歧义结果保留候选，不由适配器擅自选择。
- 错误状态不会被转换为空成功。
- 响应与日志不泄漏高德 Key。

### 测试要求

- 使用模拟高德 HTTP 响应测试公开工具结果。
- 覆盖无结果、重复名称、额度和超时。

### 依赖

Ticket 009、Ticket 012。

### 不包含

不实现地图瓦片、地点 UI 或商业许可判断。

---

## Ticket 014：实现高德路线计算适配器

### 目标

计算步行、公共交通和驾车/出租估算所需的标准路线段。

### 背景

来自 Spec F6、F9、F10。时间与空间可行性不能由模型凭常识估算。

### 修改范围

路线 API 客户端、TransportLeg 标准化、路线错误和工具注册。

### 任务

- 接受已解析地点坐标和出行模式。
- 返回时长、距离、换乘摘要、费用范围及查询时间。
- 明确理论路线时长与应用层缓冲分离。
- 处理无路线、跨城异常和服务失败。
- 不持久化完整原始响应。

### 验收标准

- 合法路线返回可用于校验的标准 TransportLeg。
- 无路线时产生明确阻塞候选，不返回零时长。
- 缓冲时间不会被双重计入。

### 测试要求

- 使用模拟结果覆盖不同模式、无路线、零距离、超时和异常时长。
- 只测试标准输出行为。

### 依赖

Ticket 013。

### 不包含

不决定最终推荐模式、不实现地图展示。

---

## Ticket 015：实现天气适配器与日期边界

### 目标

在可靠天气窗口内提供天气事实，窗口外只返回季节性/待复核状态。

### 背景

来自 Spec F6、F7、F9。未来 12 个月可规划，但不能为远期日期伪造具体天气。

### 修改范围

天气工具、日期判断、Evidence 输出和失败状态。

### 任务

- 调用适用的高德天气能力。
- 判断请求日期是否处于数据源可靠窗口。
- 可靠窗口外返回不可作为实时事实的状态。
- 标准化降雨、温度、恶劣天气和查询时间。
- 为临近复核生成结构化提醒。

### 验收标准

- 近期待测日期返回带时间的天气证据。
- 远期日期不会返回具体天气预报。
- 数据源失败时产生需复核或失败状态。

### 测试要求

- 冻结系统时间，测试窗口内、边界和窗口外行为。
- 覆盖失败与不完整天气字段。

### 依赖

Ticket 009、Ticket 012、Ticket 013。

### 不包含

不做后台天气监控或主动通知。

---

## Ticket 016：实现搜索发现与官方页面取证

### 目标

使用 DeepSeek 搜索发现候选来源，并通过安全读取器提取官方页面中的关键事实。

### 背景

来自 Spec F6、F7、7。搜索结果只能用于发现，开放时间和预约规则应尽量回到官方来源。

### 修改范围

搜索工具、页面抽取输入输出、来源分类和取证失败。

### 任务

- 将 DeepSeek web search 结果转换为候选 URL。
- 识别官方、承运方、正规平台和普通网页类别。
- 使用安全读取器获取允许的公开页面。
- 生成带原文位置/摘要、URL 和时间的 Evidence。
- 遇到登录、验证码或不可访问页面时停止并标记失败。

### 验收标准

- 搜索摘要自身不会获得“官方已核验”状态。
- 可访问官方页能产生可引用证据。
- 不可访问页面不会触发绕过或虚构内容。

### 测试要求

- 使用固定搜索和网页 fixture 测试候选分类、成功抽取和失败降级。
- 覆盖搜索结果重定向到危险 URL 的拒绝。

### 依赖

Ticket 006、Ticket 009、Ticket 011、Ticket 012。

### 不包含

不实现通用爬虫、浏览器登录或验证码处理。

---

## Ticket 017：实现平台搜索与预订跳转生成器

### 目标

为携程、飞猪等平台生成透明、可降级的搜索入口。

### 背景

来自 Spec F10、F15、7。产品不绑定账户、不读取库存，只把用户带到第三方搜索或预订页面。

### 修改范围

平台链接适配器、查询条件摘要、时间戳和后备行为。

### 任务

- 定义航班、火车、酒店和普通搜索链接输入。
- 在可支持时预填日期、城市和关键词。
- 始终保存人类可读的查询条件和生成时间。
- 深链不可用时退回普通搜索页及可复制条件。
- 标记第三方平台独立提供服务且无返佣。

### 验收标准

- 链接不包含用户密钥、个人身份或订单数据。
- 无法预填的字段不会伪装成已预填。
- 每个入口均显示查询时间和需重新确认提示。

### 测试要求

- 测试不同平台/类型生成的可解析 URL 和后备结果。
- 使用链接可达性检查的模拟结果，不依赖实时平台页面。

### 依赖

Ticket 002、Ticket 012。

### 不包含

不登录第三方账户、不读取价格/库存、不做返佣追踪。

---

## Ticket 018：实现证据归一化与冲突裁决

### 目标

把高德、搜索、官网、天气和平台结果合并成一致的 Claim/Evidence 集合。

### 背景

来自 Spec F7。多数据源会重复、缺失或矛盾，需要在规划校验前统一处理。

### 修改范围

证据归一化服务、实体匹配、冲突输出和 Claim 状态。

### 任务

- 将不同工具结果映射到统一 Claim 类型。
- 按地点、日期和字段去重。
- 使用已确认来源优先级处理可裁决冲突。
- 对不可裁决关键冲突生成 blocking/warning。
- 保留所有参与裁决的来源。

### 验收标准

- 相同事实不会重复显示为多个无关 Claim。
- 冲突不会通过覆盖旧值而消失。
- 关键未决冲突可被硬校验器识别。

### 测试要求

- 使用多来源 fixture 测试合并、优先级、日期差异和不可裁决冲突。
- 只检查对外 Claim/Evidence 结果。

### 依赖

Ticket 009、Ticket 013、Ticket 014、Ticket 015、Ticket 016、Ticket 017。

### 不包含

不生成行程、不渲染来源 UI。

---

## Ticket 019：实现需求完整性与支持范围检查

### 目标

把用户输入转换为 TripBrief，判断能否进入完整规划，并生成下一轮澄清问题。

### 背景

来自 Spec F2、2.2、2.4。完整规划不能在关键输入缺失或范围不支持时直接开始。

### 修改范围

输入解析边界、TripBrief 服务、支持范围规则和澄清问题输出。

### 任务

- 校验必填字段、3–5 天、1–6 人、未来 12 个月和城市范围。
- 识别单主城市、周边一日游和不支持的多城请求。
- 对目的地未知返回 2–3 个候选流程状态。
- 每轮最多输出 3–5 个当前可回答的高影响问题。
- 为超范围请求提供缩短、拆分或实验草案选项。

### 验收标准

- 完整输入进入 ready 状态。
- 缺失输入返回可定位问题且不启动规划。
- 超范围请求不会被标记完整支持。
- 已回答字段不会被重复询问。

### 测试要求

- 覆盖合法、缺失、目的地未知、多城、超天数、超人数和过远日期。
- 断言公开 TripBrief 和问题列表行为。

### 依赖

Ticket 002、Ticket 005。

### 不包含

不调用模型生成完整行程、不实现页面。

---

## Ticket 020：实现行程骨架规划阶段

### 目标

让 DeepSeek 基于 TripBrief 生成不依赖未核验事实的每日骨架和待取证清单。

### 背景

来自 Spec F4、F5、F9。先生成骨架可以限制搜索范围，避免模型提前编造开放时间和具体班次。

### 修改范围

骨架阶段提示、允许工具、结构化输出和阶段结果。

### 任务

- 输入 TripBrief、锁定预订和已披露默认值。
- 生成每天上午/下午核心主题、候选区域和必要交通窗口。
- 输出需要核验的地点、规则、路线、天气和费用 Claim。
- 不填充无法从输入确定的实时价格、库存和开放时间。
- 通过运行时 Schema 接收骨架。

### 验收标准

- 北京标准输入产生 3–5 天结构化骨架。
- 锁定预订出现在正确日期和时间。
- 输出包含明确取证清单。
- 骨架不存在库存或已核验声明。

### 测试要求

- 使用固定模型响应测试公开阶段输入输出。
- 覆盖锁定酒店、晚抵达、儿童和未知费用。

### 依赖

Ticket 011、Ticket 012、Ticket 019。

### 不包含

不执行外部取证、路线或最终校验。

---

## Ticket 021：实现取证、路线与预算补全阶段

### 目标

根据骨架调用必要工具，填充活动、交通、预算和备选方案。

### 背景

来自 Spec F4、F6–F10。核心行程必须建立在工具结果而非模型记忆上。

### 修改范围

Agent enrichment 阶段、工具调用计划、Evidence 关联和初版 Trip 输出。

### 任务

- 依据取证清单调用地点、路线、天气、搜索和链接工具。
- 将结果归一化为 Evidence/Claim。
- 生成活动、TransportLeg、预算分类和同区域备选。
- 应用默认密度、留白和交通模式策略。
- 区分已付款、估算范围和未知费用。

### 验收标准

- 固定北京 fixture 可生成完整初版 Trip。
- 每个关键事实可追溯到 Evidence 或明确未核验状态。
- 不可用工具不会导致虚假的零费用/零时长。

### 测试要求

- 使用固定工具响应测试完整阶段行为。
- 覆盖部分来源失败、天气窗口外、无路线和未知价格。

### 依赖

Ticket 018、Ticket 020。

### 不包含

不决定最终 lifecycle，不实现自动修复或 UI。

---

## Ticket 022：实现时间、范围与密度硬校验

### 目标

确定性检查日期、时间重叠、营业窗口、路线时长、缓冲、行程密度和支持范围。

### 背景

来自 Spec F11。时间空间可行性不能仅由模型判断。

### 修改范围

校验器公开入口、ValidationIssue 输出和相关领域测试。

### 任务

- 检查活动重叠、跨日和日期边界。
- 检查活动是否落在已核验营业/预约窗口。
- 把路线时长和场景缓冲计入时间链。
- 检查周边一日游、人数、天数和单主城市边界。
- 将过密与主动留白规则转为 warning/blocking。

### 验收标准

- 每个问题定位到实体、日期和字段。
- 无路线或无法按时抵达产生 blocking。
- 缓冲只计算一次。
- 合法样本不产生误报阻塞。

### 测试要求

- 使用完整 Trip 输入测试可达、不可达、边界时间和跨日场景。
- 不测试私有计算函数。

### 依赖

Ticket 002、Ticket 014、Ticket 021。

### 不包含

不修复问题、不检查预算或证据完整性。

---

## Ticket 023：实现锁定、预算、证据与措辞校验

### 目标

检查锁定项完整时间链、预算上限、关键证据和禁止的库存承诺。

### 背景

来自 Spec F11。模型可能间接破坏预订，也可能把估算写成确定实时值。

### 修改范围

第二组硬校验器、版本差异输入、ValidationIssue 输出。

### 任务

- 比较基线和候选 Trip 的锁定字段。
- 检查锁定项前序活动能否按时抵达。
- 计算预算分类、机动金、未知费用和上限。
- 检查关键 Claim 的证据状态和冲突。
- 检测“实时余票/库存已确认”等禁止承诺。

### 验收标准

- 直接和间接破坏锁定项均被发现。
- 未知费用不会按零计入并假装低于预算。
- 关键证据失败阻止 executable。
- 普通“请到平台确认”措辞不会被误报。

### 测试要求

- 通过公开校验接口覆盖锁定、预算、证据和措辞场景。
- 使用用户可见 ValidationIssue 断言结果。

### 依赖

Ticket 009、Ticket 021。

### 不包含

不修复问题、不修改行程。

---

## Ticket 024：实现有限修复与生命周期裁决

### 目标

把结构化校验问题返回 DeepSeek，最多修复两轮，并确定最终 TripLifecycle。

### 背景

来自 Spec F4、F11、F13。Agent 要能修复，但不能无限循环或改变硬约束。

### 修改范围

修复阶段提示、差异约束、修复计数、最终状态和替代方案。

### 任务

- 把 ValidationIssue 转为模型可执行修复请求。
- 明确禁止修改锁定和硬约束字段。
- 每轮修复后重新运行全部确定性校验。
- 两轮后仍失败则输出 blocked 及 1–2 个用户选择。
- 根据证据和错误确定 draft/checked/executable/blocked。

### 验收标准

- 修复循环最多两轮。
- 锁定项目和不受影响日期保持不变。
- 所有硬门槛通过才可 executable。
- 失败状态包含可理解原因而非空结果。

### 测试要求

- 使用固定修复响应测试一次成功、两次失败、越权修改和无变化修复。
- 断言外部 Trip、issues 和 lifecycle。

### 依赖

Ticket 011、Ticket 022、Ticket 023。

### 不包含

不提供页面修改交互或任务执行循环。

---

## Ticket 025：实现可恢复 Agent 任务执行器

### 目标

串联需求、骨架、取证、校验、修复和最终化阶段，并持久化进度。

### 背景

来自 Spec F4、5。完整规划是长任务，需要可取消、可恢复、可观察。

### 修改范围

后台执行器、阶段调度、幂等工具调用、重启恢复和凭据生命周期。

### 任务

- 根据 Job 状态执行下一阶段。
- 每阶段前后原子持久化状态。
- 为外部调用生成幂等标识。
- 响应取消，不启动后续阶段。
- 重启后恢复确定性阶段；需要模型时无 Key 则等待凭据。
- 汇总耗时、Token、费用和工具调用数。

### 验收标准

- 固定 provider 下任务可端到端完成。
- 重复启动同一任务不会重复已确认外部调用。
- 重启与 Key 丢失产生正确等待状态。
- 取消任务不会继续计费。

### 测试要求

- 使用模拟时钟和 provider 测试完成、取消、重启、重复调度和等待凭据。
- 只检查任务及外部调用结果。

### 依赖

Ticket 008、Ticket 019–Ticket 024。

### 不包含

不提供 HTTP、SSE 或前端页面。

---

## Ticket 026：实现任务 HTTP、SSE、轮询、恢复与取消接口

### 目标

向浏览器公开任务创建、状态、进度事件、恢复和取消能力。

### 背景

来自 Spec 6、F4。用户应在 2 秒内看到进度，SSE 失败时可轮询。

### 修改范围

`/api/planning-jobs` 路由、SSE 事件、轮询响应、权限令牌和错误契约。

### 任务

- 创建任务并返回匿名访问令牌。
- 返回当前阶段、可展示进度、费用摘要和错误。
- 提供 SSE，支持断线后通过事件序号恢复。
- 提供等价轮询结果。
- 恢复请求临时接收 Key，不持久化。
- 实现取消幂等性。

### 验收标准

- 创建后 2 秒内可获得至少一个进度状态。
- SSE 重连不重复或遗漏已编号事件。
- 无权限令牌不能读取/取消任务。
- 恢复后 Key 不出现在 Job 数据中。

### 测试要求

- HTTP 集成测试覆盖创建、查询、SSE 重连、轮询、恢复、取消和未授权。
- 使用模拟执行器，不依赖网络。

### 依赖

Ticket 010、Ticket 025。

### 不包含

不实现前端进度页面或分享链接。

---

## Ticket 027：首页、导航与公开信息架构

### 目标

建立首页和“固定案例/自定义规划/案例研究/关于”导航入口。

### 背景

来自 Spec F1、F12、F18。首次访问者需要立即理解产品，而不是面对空白聊天框。

### 修改范围

首页、全局布局、导航、响应式基础和公开文案。

### 任务

- 实现两个主要 CTA。
- 明确固定案例与真实生成的区别。
- 展示支持范围和“不代订、不承诺库存”提示。
- 增加案例研究和关于页占位入口。
- 建立克制、时间线优先的视觉基线。

### 验收标准

- 用户不输入 Key 即可进入北京案例入口。
- 自定义规划入口明确说明 BYOK。
- 页面不使用携程等第三方品牌冒充产品名称。

### 测试要求

- 浏览器测试两个 CTA 和导航目标。
- 检查核心边界文案对用户可见。

### 依赖

Ticket 001。

### 不包含

不实现案例内容、规划表单或正式品牌设计。

---

## Ticket 028：自定义规划输入、旅行简报与 Key 体验

### 目标

让用户填写旅行信息、查看结构化简报、验证/清除 Key，并在满足条件后开始规划。

### 背景

来自 Spec F2、F3。用户必须看见 Agent 当前理解，Key 必须走专用安全入口。

### 修改范围

规划入口页面、TripBrief 表单/对话区域、Key 内存状态和验证接口集成。

### 任务

- 提供自然语言输入和关键快捷字段。
- 展示可编辑旅行简报、缺失项和默认假设。
- 每轮显示 3–5 个澄清问题。
- 实现 Key 密码输入、验证状态和立即清除。
- 满足条件后调用任务创建接口。

### 验收标准

- 缺失关键项不能开始完整规划。
- 修改简报会更新下一轮问题。
- Key 刷新后消失且不进入浏览器持久化。
- 聊天框粘贴疑似 Key 会被阻止。

### 测试要求

- E2E 覆盖输入、缺失提示、编辑、Key 验证失败/成功、清除和启动。
- 使用模拟验证/任务 API。

### 依赖

Ticket 004、Ticket 005、Ticket 019、Ticket 026、Ticket 027。

### 不包含

不渲染最终行程、不支持账户登录。

---

## Ticket 029：任务进度、取消和凭据恢复界面

### 目标

展示用户可理解的规划阶段，并支持 SSE、轮询后备、取消和重新提供 Key。

### 背景

来自 Spec F4、F12。不能只显示加载动画，也不能暴露原始模型推理。

### 修改范围

任务运行页面、事件订阅、轮询、错误/等待状态和恢复表单。

### 任务

- 显示六类用户进度文案。
- SSE 断线时切换轮询并可恢复 SSE。
- 显示取消操作和终态。
- `waiting_for_credentials` 时重新请求 Key。
- 显示可理解的失败、费用/步骤上限和重试建议。

### 验收标准

- 正常任务阶段按事件更新。
- 断线不会丢失最终结果。
- 取消后 UI 不继续显示运行。
- 恢复 Key 不被保存。
- 页面不显示 reasoning 原文或内部提示词。

### 测试要求

- E2E 使用模拟事件覆盖正常、断线、轮询、取消、等待凭据和失败。

### 依赖

Ticket 026、Ticket 028。

### 不包含

不实现任务执行器或最终行程视图。

---

## Ticket 030：行程时间线与简化路线总览

### 目标

把 Trip 渲染为桌面优先、移动可读的每日时间线和非地图瓦片路线总览。

### 背景

来自 Spec F12。时间线是主要工作区，地图许可未确认前只展示简化空间关系。

### 修改范围

行程结果页、每日分组、Activity/TransportLeg 视图和简化路线组件。

### 任务

- 渲染日期、时间窗、活动、交通、缓冲和留白。
- 区分核心、可选、锁定和备选项目。
- 展示地点顺序和逐段外部高德入口占位。
- 支持桌面双栏和移动单栏。
- 从同一 Trip 对象读取，不生成独立文本副本。

### 验收标准

- 北京 fixture 的全部活动和交通按日正确显示。
- 未知时间/费用有明确表示。
- 不嵌入高德地图瓦片。
- 移动视口不丢失核心内容。

### 测试要求

- 组件/浏览器测试从公开页面验证活动顺序、状态、锁定和移动布局。
- 不测试私有组件层级。

### 依赖

Ticket 002、Ticket 027。

### 不包含

不实现编辑、证据抽屉或交互地图。

---

## Ticket 031：预算、证据、状态与阻塞提醒界面

### 目标

展示预算分类、Evidence 状态、查询时间、来源冲突和顶部阻塞提醒。

### 背景

来自 Spec F7、F10–F12。用户必须在决策位置理解哪些内容已经核验。

### 修改范围

结果页预算区域、来源标签、证据详情、提醒汇总和 lifecycle 状态。

### 任务

- 显示已付款、估算范围、未知和机动金。
- 每个关键项目显示 verified/recheck/failed。
- 展开查看来源、查询时间和冲突。
- 将 blocking 问题集中置顶。
- 解释 draft/checked/executable/blocked。

### 验收标准

- 关键失败不会只藏在详情中。
- 未知费用不显示为零。
- 来源 URL、时间和状态与 Trip 数据一致。
- checked 与 executable 有可见差异。

### 测试要求

- 使用不同生命周期 fixture 做页面行为测试。
- 覆盖来源冲突、查询失败和预算超限。

### 依赖

Ticket 009、Ticket 030。

### 不包含

不重新判断证据真伪、不实现编辑。

---

## Ticket 032：直接编辑、锁定与版本记录

### 目标

支持无连锁影响的字段编辑、锁定/解锁和本地版本记录。

### 背景

来自 Spec F13。小修改不应每次调用模型，但所有修改都需保持 Schema 和版本一致。

### 修改范围

结果页编辑控件、TripRevision、浏览器存储和本地校验。

### 任务

- 编辑允许的时间窗、备注、活动状态等字段。
- 锁定/解锁活动、交通和预订。
- 每次提交生成新 version 和 revision 摘要。
- 对明显会产生连锁影响的修改转入预览流程。
- 保存到浏览器并支持撤销最近修改。

### 验收标准

- 小修改立即反映并在刷新后保留。
- 锁定状态可见且进入 Trip 数据。
- 非法时间或 Schema 破坏不提交。
- 修改不改变无关实体 ID。

### 测试要求

- E2E 覆盖编辑、锁定、刷新恢复、撤销和非法输入。
- 只验证用户可见结果和保存的 Trip。

### 依赖

Ticket 004、Ticket 022、Ticket 023、Ticket 030。

### 不包含

不执行模型重规划、不实现复杂跨日拖拽。

---

## Ticket 033：重大修改预览与局部重规划

### 目标

预览跨时段、预算、锁定或多日变化的影响，确认后调用局部重规划。

### 背景

来自 Spec F13、6。Agent 必须保留不受影响部分，并让用户决定重大取舍。

### 修改范围

change-preview/replan API、差异模型、影响视图和确认提交。

### 任务

- 计算受影响实体、日期、预算和锁定冲突。
- 小改动返回可直接应用结果；重大改动返回预览。
- 构造只含相关上下文的局部重规划任务。
- 确认后创建新 Trip version。
- 失败时返回 1–2 个用户选项。

### 验收标准

- 未确认的重大改动不会修改原 Trip。
- 确认后不受影响日期保持逐字段一致。
- 锁定项目不能被模型改写。
- 无 Key 时自由重规划进入凭据请求。

### 测试要求

- 集成/E2E 覆盖跨日移动、预算变化、锁定冲突、确认、取消和修复失败。

### 依赖

Ticket 024、Ticket 025、Ticket 026、Ticket 032。

### 不包含

不实现后台主动监控或无限撤销历史。

---

## Ticket 034：建立北京四日规范案例数据

### 目标

创建可复现、经过人工核验结构设计的北京四日 Trip fixture。

### 背景

来自 Spec F14。固定案例是无 Key 体验、开发基线和评测核心。

### 修改范围

种子数据、来源快照元数据、案例加载接口和 fixture 校验。

### 任务

- 建立完整 TripBrief、锁定项、四日时间线、交通、预算、证据和备选。
- 标注数据核验日期和固定案例属性。
- 确保通过全部确定性校验。
- 提供只读基线和创建个人副本接口。
- 不包含真实用户信息。

### 验收标准

- fixture 可通过 Trip Schema 和硬校验。
- 无网络、无 Key 时可以加载。
- 创建副本后修改不会影响基线。
- 页面明确标识预设、非实时生成。

### 测试要求

- 固定案例解析、校验、复制隔离和无网络加载测试。

### 依赖

Ticket 018、Ticket 022、Ticket 023。

### 不包含

不实现自由模型生成或其他城市案例。

---

## Ticket 035：实现无 Key 的预设重规划场景

### 目标

提供 2–3 个预先验证、确定性的北京案例变化。

### 背景

来自 Spec F14。招聘者不注册 DeepSeek 也应看到 Agent 式重规划效果，但不能伪装成实时模型调用。

### 修改范围

预设场景数据、场景选择 UI、差异展示和一键恢复。

### 任务

- 至少覆盖下雨、未预约成功和临时疲劳/酒店变化中的三个。
- 为每个场景准备输入、影响预览和校验后的结果。
- 显示这是预先验证情境。
- 支持一键恢复公共案例基线。

### 验收标准

- 无 Key、无网络时每个场景可完成。
- 场景结果不破坏锁定项并通过硬校验。
- 用户自由文本不会被当成预设模型结果。

### 测试要求

- E2E 覆盖每个场景、影响预览、应用和恢复。

### 依赖

Ticket 030–Ticket 034。

### 不包含

不提供任意无 Key 自由对话或产品方模型补贴。

---

## Ticket 036：实现只读分享与删除令牌

### 目标

创建字段可选、默认隐私保护、30 天过期的只读分享页。

### 背景

来自 Spec F15。分享必须与本地匿名行程分离，并防止枚举、修改和搜索收录。

### 修改范围

分享 API、ShareRecord、令牌生成/哈希、分享页、过期和删除。

### 任务

- 创建前预览分享字段。
- 默认排除聊天、私人备注和预算。
- 生成独立只读令牌与删除令牌，只存哈希。
- 设置 30 天到期和 `noindex`。
- 删除令牌立即使分享不可访问。

### 验收标准

- 查看令牌不能修改或删除。
- 无效、过期、删除后的链接返回不可用而不泄漏内容。
- 分享页面只包含用户选择字段。
- 令牌不可通过连续 ID 枚举。

### 测试要求

- HTTP/E2E 覆盖创建、字段过滤、读取、过期、删除、错误令牌和 noindex。

### 依赖

Ticket 003、Ticket 005、Ticket 030、Ticket 031。

### 不包含

不提供公开广场、搜索、协作编辑或用户账户。

---

## Ticket 037：实现确定性 PDF 导出

### 目标

从 Trip 生成可打印 PDF，包含时间线、预算选择、来源提示和 AI 标识。

### 背景

来自 Spec F15。导出不能让模型重新改写，也不能泄漏未选择字段。

### 修改范围

PDF 渲染器、导出 API、字段选择、元数据和视觉验收 fixture。

### 任务

- 由结构化 Trip 确定性渲染。
- 支持隐藏预算和私人字段。
- 添加显式 AI 标识及支持的文件元数据标识。
- 处理中文字体、分页、长来源和失败。
- 不嵌入未获许可的地图截图。

### 验收标准

- PDF 内容与页面 Trip 一致。
- 中文无乱码，关键时间线不被错误截断。
- 隐藏字段不出现在文件文本中。
- 文件含可见 AI 标识。

### 测试要求

- 测试导出 HTTP 行为和 PDF 文本/元数据。
- 对代表性页面做渲染快照或人工视觉检查。

### 依赖

Ticket 002、Ticket 030、Ticket 031。

### 不包含

不导出聊天全文、不嵌入地图瓦片。

---

## Ticket 038：实现 ICS 与外部行动链接导出

### 目标

从 Trip 生成 `.ics`，并在页面/导出中提供高德及平台行动链接。

### 背景

来自 Spec F15。日历和链接是执行辅助，不是订单或库存系统。

### 修改范围

ICS 生成器、导出 API、时区、活动映射和外部链接渲染。

### 任务

- 使用 `Asia/Shanghai` 生成有时间活动。
- 对弹性时间窗采用明确描述，不伪造精确预约。
- 添加地点、备注、来源提示和 AI 标识文本。
- 使用已生成的平台和高德链接。
- 对缺少时间的项目采取明确跳过/全天事件策略。

### 验收标准

- 标准日历解析器可以读取文件。
- 时间、日期和地点与 Trip 一致。
- 未核验事项在描述中保留提示。
- 不包含密钥或私人隐藏字段。

### 测试要求

- 解析生成的 ICS 并验证公开事件字段。
- 覆盖跨日、弹性时间、中文地点和未知时间。

### 依赖

Ticket 017、Ticket 030、Ticket 031。

### 不包含

不创建第三方日历账户、不发送通知。

---

## Ticket 039：实现 AI 标识、模型信息与关于页

### 目标

在生成界面、分享和导出中显示 AI 标识，并在关于页预留经核实的模型备案信息。

### 背景

来自 Spec 8。生成界面只显示模型名称；服务提供方和备案/上线信息放在关于或产品详情页。

### 修改范围

全局生成标识、关于页、模型信息配置、分享/PDF/ICS 联动。

### 任务

- 生成页面显示 DeepSeek 模型名称和“AI 辅助生成”。
- 关于页显示服务方、隐私边界和可配置备案/上线字段。
- 未核实时显示“待核实”，不得填猜测编号。
- 分享和导出继承显式标识。
- 文案说明不代订、不保证库存和需复核关键事实。

### 验收标准

- 所有生成内容入口可见 AI 标识。
- 关于页不会在编号缺失时伪造值。
- 生成界面不额外展示完整服务方/备案详情。

### 测试要求

- 页面/E2E 测试不同配置状态的可见文案。
- 验证分享、PDF、ICS 中标识存在。

### 依赖

Ticket 027、Ticket 036–Ticket 038。

### 不包含

不决定法律适用性、不自行申请或填写备案编号。

---

## Ticket 040：实现数据过期清理与备份接口

### 目标

执行临时任务 24 小时、分享 30 天、指标 30 天的清理，并建立每日加密备份能力。

### 背景

来自 Spec F17、8。保存期限必须成为自动行为，而不是文档承诺。

### 修改范围

清理任务、过期查询、备份/恢复命令、轮换和运行记录。

### 任务

- 分类别删除到期主数据。
- 用户主动删除优先于自然到期。
- 生成加密备份并保留 7 天。
- 备份失败产生脱敏运维错误。
- 提供恢复到临时数据库的验证命令。

### 验收标准

- 各类到期数据按期限不可访问。
- 删除数据最多随 7 天备份轮换消失。
- 备份不包含 DeepSeek Key。
- 恢复验证不会覆盖当前主库。

### 测试要求

- 使用冻结时钟和临时目录测试清理、轮换、失败及恢复副本。

### 依赖

Ticket 003、Ticket 036。

### 不包含

不选择最终云备份供应商或密钥托管产品。

---

## Ticket 041：实现匿名限流与功能开关中间件

### 目标

把匿名会话、每日轮换 IP 哈希、功能开关和费用护栏应用到真实接口。

### 背景

来自 Spec F17。公开受限测试仍需防止服务端和高德额度被滥用。

### 修改范围

API 中间件、哈希轮换、限流存储、标准错误和接口接入。

### 任务

- 组合匿名会话与当日 IP 哈希。
- 不保存原始 IP 或稳定跨日标识。
- 为 Key 验证、任务、搜索、高德、分享和导出设置独立限制。
- 接入功能开关和月度硬停止。
- 返回重试时间与不泄密错误。

### 验收标准

- 超限请求不触发外部调用。
- 次日哈希无法直接关联前一日。
- 关闭单一功能不影响其他读取功能。
- 固定案例不受模型生成硬停止影响。

### 测试要求

- HTTP 测试覆盖阈值、轮换、不同接口、功能开关和并发。
- 检查持久化数据不含原始 IP。

### 依赖

Ticket 007、Ticket 026、Ticket 036–Ticket 038。

### 不包含

不建立用户账户、设备指纹或管理后台。

---

## Ticket 042：实现隐私型分析与用户反馈

### 目标

记录脱敏产品事件，并提供点赞/点踩、原因标签和用户主动评论。

### 背景

来自 Spec F17、F18。需要衡量开始、完成、编辑、分享、导出和错误，但不能记录完整聊天或行程正文。

### 修改范围

事件 Schema、分析写入、反馈 UI、保存期限和删除能力。

### 任务

- 定义无正文事件及允许字段白名单。
- 记录任务阶段、耗时、结果、编辑、分享和导出事件。
- 实现点赞/点踩、原因标签和可选评论。
- 提交前说明用途，评论需用户主动输入。
- 30 天清理并支持删除反馈。

### 验收标准

- 分析数据不含聊天、完整 Trip、Key、原始 IP 或证件信息。
- 未提交反馈不会自动上传正文。
- 用户可以提交并删除自己的反馈记录。

### 测试要求

- 通过公开事件输出检查字段白名单和敏感数据拒绝。
- E2E 覆盖反馈提交、取消和删除。

### 依赖

Ticket 003、Ticket 005、Ticket 040。

### 不包含

不接入全量会话回放或广告追踪。

---

## Ticket 043：完成移动端、键盘与无障碍基线

### 目标

保证核心页面在主要浏览器和移动视口可用，并满足已确认的键盘、焦点、对比度和减少动画要求。

### 背景

来自 Spec F12、2.6。桌面优先，但旅行途中需要移动查看和基础编辑。

### 修改范围

首页、输入、进度、结果、分享和编辑页面的响应式与可访问行为。

### 任务

- 建立桌面双栏与移动单栏规则。
- 完成键盘导航、焦点顺序、标签和错误关联。
- 支持系统减少动画设置。
- 检查颜色对比度和触控目标。
- 对当前及前一个主要版本建立浏览器测试矩阵。

### 验收标准

- 无鼠标可完成输入、查看、锁定、分享和导出。
- 关键错误可被辅助技术关联到字段。
- 移动端无横向溢出或不可达操作。

### 测试要求

- 自动可访问性扫描加关键键盘 E2E。
- 主要桌面/移动视口截图与行为测试。

### 依赖

Ticket 028–Ticket 039。

### 不包含

不承诺所有旅行地点物理无障碍，不做完整视觉品牌重设计。

---

## Ticket 044：实现可裁剪的只读离线查看

### 目标

允许已保存 Trip 在有限离线场景下只读打开，同时保持该能力可从 MVP 裁剪。

### 背景

来自 Spec F16、12。离线只读是次要能力，不得阻塞核心 Agent 闭环。

### 修改范围

应用壳缓存、浏览器 Trip 读取、离线状态提示和禁用操作。

### 任务

- 缓存打开已保存 Trip 所需的最小应用资源。
- 离线时只读展示本地 Trip。
- 禁用需要网络的重规划、链接核验、分享和导出操作。
- 清楚显示数据的最后更新时间。
- 通过功能开关允许完全关闭该能力。

### 验收标准

- 有本地 Trip 时断网可打开只读视图。
- 无本地数据时显示明确离线错误。
- 离线不会伪装成已更新事实。

### 测试要求

- E2E 模拟有/无本地数据的离线访问及禁用操作。

### 依赖

Ticket 004、Ticket 030、Ticket 031、Ticket 043。

### 不包含

不支持离线编辑、冲突合并或多端同步。

---

## Ticket 045：建立故障注入与恢复回归套件

### 目标

验证 DeepSeek、高德、官网、数据库、SSE 和导出失败时的可见降级与状态保留。

### 背景

来自 Spec 10.2。Agent 的可信度取决于失败时不编造、不重复计费和不丢状态。

### 修改范围

故障 provider、集成测试 fixture、容器/数据库故障模拟和恢复断言。

### 任务

- 模拟 DeepSeek 429、超时、非法 JSON 和截断。
- 模拟高德超时、额度、无路线。
- 模拟官网 404、结构变化和来源冲突。
- 模拟 SQLite 写失败、SSE 断线、进程重启和导出失败。
- 验证 Key 丢失进入等待凭据。

### 验收标准

- 每类故障产生明确公开状态。
- 不产生伪造成功、重复外部调用或丢失锁定项。
- 可恢复故障能够从最后安全阶段继续。

### 测试要求

- 本 ticket 本身即集成/故障测试套件。
- 测试只断言 HTTP、Job、Trip 和用户可见状态。

### 依赖

Ticket 025、Ticket 026、Ticket 037、Ticket 038、Ticket 040。

### 不包含

不进行生产混沌工程或破坏真实外部账户。

---

## Ticket 046：完成隐私与安全端到端回归

### 目标

从完整产品边界证明 Key、个人信息、分享令牌和外部 URL 安全要求成立。

### 背景

来自 Spec F3、F17 和“不做什么”。局部单元测试不足以证明敏感数据未从其他路径泄漏。

### 修改范围

`test:security`、数据库/日志扫描、HTTP 安全测试和分享攻击用例。

### 任务

- 执行带标记测试 Secret 的完整用户流程。
- 扫描数据库、日志、分析事件、URL、错误和导出。
- 测试分享令牌枚举、查看/删除权限分离和 noindex。
- 测试 SSRF、重定向、超长输入和工具调用上限。
- 验证功能开关与月度硬停止。

### 验收标准

- 测试 Secret 在所有禁止位置零命中。
- 攻击性请求被拒绝且不触发外部调用。
- 分享权限和过期规则成立。
- `npm run test:security` 可重复运行。

### 测试要求

- 本 ticket 建立外部行为安全套件，不断言私有函数。

### 依赖

Ticket 039–Ticket 045。

### 不包含

不替代第三方渗透测试或正式法律审计。

---

## Ticket 047：建立固定响应 Agent 评测框架

### 目标

实现可复现的案例运行、关键失败检测、软评分和报告生成。

### 背景

来自 Spec 10.1。标准评测必须独立于实时网络，并公开样本数和失败。

### 修改范围

`evals` 运行器、fixture provider、评分 Schema、报告和命令。

### 任务

- 定义案例输入、工具 fixture、预期硬门槛和人工评分字段。
- 自动检测六类关键失败。
- 实现软评分权重和平均分。
- 支持每案例重复运行和随机种子/响应版本记录。
- 输出机器可读与 Markdown 报告，保留失败样本。

### 验收标准

- 同一固定 fixture 重复运行产生一致报告。
- 关键失败不会被软分抵消。
- 报告显示模型、日期、样本数、通过率和失败详情。

### 测试要求

- 用已知成功/失败的小型案例验证评分和门槛。
- `npm run test:eval:fixtures` 无网络运行。

### 依赖

Ticket 022–Ticket 025。

### 不包含

不创建完整 12 案例，不调用真实网络。

---

## Ticket 048：建立 12 个标准评测案例与工具 fixtures

### 目标

创建约 8 个北京变体和上海、重庆、西安、杭州各 1 个案例。

### 背景

来自 Spec 10.1。案例需覆盖儿童、老人、预算、下雨、闭馆、晚抵达、锁定预订和来源故障。

### 修改范围

评测数据、固定工具响应、预期硬门槛、案例说明和隐私检查。

### 任务

- 建立 12 个完整 TripBrief。
- 为每个案例准备地点、路线、天气、官网和平台固定结果。
- 明确期望通过项、必须阻塞项和禁止行为。
- 覆盖五个首批城市及已确认边界。
- 确保 fixture 不含真实个人信息和未经许可的大量原始地图数据。

### 验收标准

- 12 个案例均可离线运行。
- 每个风险主题至少被一个案例覆盖。
- 案例清单与预期结果可供人工审阅。

### 测试要求

- 运行 Schema、引用完整性和覆盖矩阵检查。
- 运行一次完整 fixture 评测作为冒烟。

### 依赖

Ticket 034、Ticket 047。

### 不包含

不声称 fixture 代表实时开放信息，不执行 36 次正式结果。

---

## Ticket 049：实现真实联网评测运行器与证据留档

### 目标

允许显式使用内部测试 Key 对少量案例执行真实 DeepSeek、高德和官方来源验收。

### 背景

来自 Spec 2.7、10。固定测试发现代码回归，联网测试发现供应商和数据源变化。

### 修改范围

`test:eval:live`、凭据门槛、运行预算、证据摘要和结果隔离。

### 任务

- 默认拒绝在未显式启用时联网。
- 要求内部 DeepSeek 和产品高德测试凭据。
- 设置单次运行案例数、步骤和费用硬上限。
- 保存脱敏结果、来源 URL、查询时间和模型信息。
- 不记录测试者 Key 或私人输入。

### 验收标准

- 普通 CI 不会执行联网评测。
- 显式运行可生成独立、带时间的报告。
- 超预算时安全停止并保留已完成结果。

### 测试要求

- 用本地模拟网络验证启用门槛、预算和报告。
- 真实调用作为人工受控验收，不作为默认自动测试。

### 依赖

Ticket 010–Ticket 018、Ticket 047、Ticket 048。

### 不包含

不使用访客 Key、不持续监控外部网站。

---

## Ticket 050：建立性能与费用验收

### 目标

测量进度首响、完整任务耗时、Token、用户模型费用估算和产品方费用护栏。

### 背景

来自 Spec 9。目标是 2 秒进度、75 秒中位、150 秒 P95、180 秒恢复边界及费用阈值。

### 修改范围

性能测试、指标聚合、报告和超时/预算断言。

### 任务

- 记录任务创建到首个进度事件时间。
- 记录端到端完成/阻塞耗时。
- 聚合 P50/P95、Token 和估算费用。
- 测试 180 秒处理和月度 300/500 元状态。
- 区分固定 provider 性能与受控联网观察。

### 验收标准

- 报告可判断所有 Spec 阈值是否通过。
- 费用分类不混合访客与产品方支出。
- 超时后任务处于可恢复或明确终态。

### 测试要求

- 固定延迟 provider 进行自动阈值测试。
- 联网性能只作为受控报告，避免 CI 波动。

### 依赖

Ticket 026、Ticket 047–Ticket 049。

### 不包含

不承诺香港节点等同大陆正式性能。

---

## Ticket 051：实现 DeepSeek 模型更新回归闸门

### 目标

检测记录到的 DeepSeek 模型变化，运行 5 个冒烟案例并控制自定义生成开关。

### 背景

来自 Spec 10.4。官方模型别名滚动更新，不能假设行为恒定。

### 修改范围

模型版本记录、变更检测、冒烟案例集合、回归状态和功能开关联动。

### 任务

- 保存最近通过评测的模型标识和日期。
- 发现变化时标记 `regression_required`。
- 运行 5 个核心冒烟案例。
- 出现关键失败时关闭自定义生成但保留固定案例和读取。
- 完整 36 次通过后允许恢复/扩大开放。

### 验收标准

- 未变化不会重复触发闸门。
- 变更且失败时生成接口被关闭。
- 管理通过配置/命令完成，不建设管理后台。
- 回归状态可在运维信息中查看。

### 测试要求

- 使用模拟模型标识和评测结果测试不变、变化通过、变化失败及恢复。

### 依赖

Ticket 007、Ticket 041、Ticket 047、Ticket 048。

### 不包含

不实现多模型回退或静默切换。

---

## Ticket 052：实现公开案例研究与评测结果页

### 目标

展示问题、决策、架构、数据来源、评测、失败样本和已知限制。

### 背景

来自 Spec F18。目标受众需要看到产品方法和证据，而非只有漂亮行程。

### 修改范围

案例研究页面、评测报告读取、架构图/流程说明和隐私过滤。

### 任务

- 展示产品目标、范围和“不做什么”。
- 展示 Agent 状态机和确定性校验关系。
- 读取已生成评测报告，不手写虚假指标。
- 显示样本数、模型、日期、通过率、失败和限制。
- 链接固定北京案例。

### 验收标准

- 没有评测结果时显示“尚未完成”，不显示占位成功率。
- 有结果时成功和失败样本都可见。
- 页面不包含私人行程、Key 或原始思维链。

### 测试要求

- 页面测试无报告、通过报告和含失败报告三种状态。
- 检查核心透明度字段可见。

### 依赖

Ticket 027、Ticket 039、Ticket 047、Ticket 048。

### 不包含

不生成营销夸大文案、不修改评测结果。

---

## Ticket 053：建立 Docker、健康检查与香港受限部署配置

### 目标

把 Web/API/执行器作为单容器部署，并提供健康检查、持久卷和安全配置说明。

### 背景

来自 Spec 2.6、8、12。当前仅部署受限演示，不把香港节点视为大陆正式发布。

### 修改范围

Dockerfile、运行入口、健康检查、SQLite 卷、反向代理要求和部署文档。

### 任务

- 构建生产容器并运行 Web/API/任务执行器。
- 配置非 root 运行、只读文件系统边界和持久数据目录。
- 提供 liveness/readiness 健康检查。
- 记录必要环境变量，不包含真实值。
- 说明 HTTPS、备份、日志和重启恢复要求。

### 验收标准

- 新环境可按文档构建并启动。
- 重启容器后 SQLite 数据保留，BYOK Key 不保留。
- 健康检查能区分存活与不可服务。
- 生产镜像不包含测试 Secret。

### 测试要求

- 自动构建镜像并执行首页、健康检查和重启持久化冒烟。
- 扫描镜像和配置中的测试密钥标记。

### 依赖

Ticket 025、Ticket 026、Ticket 040、Ticket 041。

### 不包含

不选择/购买云主机、不完成 ICP 或生成式 AI 登记。

---

## Ticket 054：准备真实用户测试支持包

### 目标

提供 5 人用户测试所需的任务脚本、反馈入口、脱敏结果模板和汇总工具。

### 背景

来自 Spec 10.3。真实用户测试是完成门槛，但招募和执行需要项目负责人参与。

### 修改范围

测试说明、计时/事件映射、反馈结果 Schema、匿名汇总和报告模板。

### 任务

- 定义“修改北京案例”和“规划自己的旅行”任务脚本。
- 定义是否需要指导、可接受方案时间和关键修改时间。
- 复用隐私型分析和反馈入口。
- 生成不含旅行正文的汇总报告。
- 提供参与者告知与删除反馈方式。

### 验收标准

- 可以记录 5 名参与者的完成/未完成和时间指标。
- 报告可判断 4/5、8 分钟和 2 分钟门槛。
- 不要求记录姓名或完整旅行内容。

### 测试要求

- 使用虚构参与者数据测试汇总、缺失结果和删除。
- E2E 验证反馈流程。

### 依赖

Ticket 035、Ticket 042、Ticket 043。

### 不包含

不招募、冒充或代替真实用户，不伪造测试结果。

---

## Ticket 055：完善工程文档、架构说明与运行手册

### 目标

让新开发者能安装、运行、测试、评测、部署和处理常见故障。

### 背景

来自 Spec 13。README、架构说明、评测结果和案例页是交付组成部分。

### 修改范围

README、架构文档、数据流、威胁边界、环境变量、测试与运维手册。

### 任务

- 更新实际安装和运行命令。
- 说明 BYOK Key 生命周期和恢复限制。
- 说明 Agent 阶段、工具、Schema、校验和数据期限。
- 说明固定/联网评测及模型回归流程。
- 说明功能开关、备份恢复和故障处理。
- 链接 Spec、Tickets 和案例研究。

### 验收标准

- 新环境只按文档可运行无网络测试和构建。
- 文档与实际脚本、接口和状态一致。
- 不包含真实密钥、私人数据或虚假合规结论。

### 测试要求

- 在干净环境执行文档中的非联网命令。
- 自动检查文档内部链接和命令名称存在。

### 依赖

Ticket 050–Ticket 054。

### 不包含

不改变 Spec，不补写虚假的评测或备案信息。

---

## Ticket 056：执行最终发布门槛与仓库公开检查

### 目标

以 Spec 的 MVP 完成定义执行最终 go/no-go 检查，并准备安全公开源代码。

### 背景

来自 Spec 13。页面部署成功不等于产品完成，所有关键门槛必须有证据。

### 修改范围

发布检查脚本、验收清单、仓库敏感信息扫描、最终报告和功能开放状态。

### 任务

- 运行 lint、typecheck、全部非联网测试和生产构建。
- 运行 36 次固定评测并读取门槛。
- 纳入受控联网、性能、安全和真实用户报告。
- 扫描当前文件及 Git 历史中的密钥和私人数据。
- 检查五城支持标签、AI 标识、已知限制和外部事实状态。
- 未满足关键条件时输出 no-go，不扩大自定义生成。

### 验收标准

- 关键失败为 0，成功率和软评分达到 Spec。
- 安全、性能、成本和用户测试有可追溯结果。
- 公开仓库不含真实密钥或私人内容。
- 未完成的备案/许可不会被标记为完成。

### 测试要求

- 发布检查自身用通过/失败 fixture 验证 go/no-go 行为。
- 最终执行所有 Spec 命令并保存脱敏报告。

### 依赖

Ticket 046、Ticket 048–Ticket 055。

### 不包含

不绕过失败门槛、不自动购买服务、不办理法律手续、不开放未获授权功能。

---

## 需要确认的问题

以下不是产品需求缺口，不阻塞前置 tickets；它们是执行后期任务前必须由项目负责人提供或确认的外部输入：

1. **内部联网评测 Key**：谁提供用于 Ticket 049 的 DeepSeek 测试 Key，以及允许的单次/月度额度。
2. **高德开发者主体与许可**：谁申请产品高德 Key；当前个人/企业认证类型；公开展示、路线摘要、缓存和商业使用许可边界。阻塞 Ticket 013–015 的真实联网验收及正式发布。
3. **香港部署资源**：具体云服务商、账号、区域、域名、预算和备份目标。阻塞 Ticket 053 的真实部署，不阻塞本地容器实现。
4. **备份加密密钥管理**：备份落点和密钥由谁保管。阻塞 Ticket 040 的生产配置，不阻塞本地备份接口。
5. **合规主体**：ICP备案和生成式 AI 应用登记使用个人还是企业主体，由谁提交和维护。阻塞大陆正式发布，不阻塞固定案例与定向测试。
6. **DeepSeek公示信息**：正式发布前需从官方登记渠道核实准确的模型备案号或上线编号。Ticket 039 在核实前只能显示“待核实”。
7. **真实用户测试执行**：由谁招募 5 名目标用户、安排测试并取得其主动反馈。Ticket 054 只提供工具和流程，不伪造参与者结果。
8. **公开仓库位置**：最终使用的 Git 托管平台、组织/个人账号及仓库可见性切换时间。阻塞 Ticket 056 的实际公开，不阻塞代码和安全检查。
